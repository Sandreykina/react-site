import React from "react";

const Contacts = () => {
    return (
        <div>
            <main className="content">
                <div className="contacts">
                    <h1>Контакты</h1>
                </div>
            </main>
        </div>
    );
}

export default Contacts;
